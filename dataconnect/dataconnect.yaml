specVersion: "v2"
serviceId: "portaleb-service"
location: "europe-southwest1"
schema:
  source: "schema"
  datasource:
    postgresql:
      database: "portaleb-database"
      cloudSql:
        instanceId: "portaleb-instance"
connectorDirs: ["connector"]
